# Folder FXML (UI Layout)
Tempat menyimpan semua file desain antarmuka `.fxml` dari Scene Builder.

## File yang akan didesain:
- `login.fxml`
- `dashboard.fxml` (Tabel pencarian, filter status aktif/selesai, tombol sorting)
- `checkin.fxml` (Form input kelompok & searchable dropdown sampah)